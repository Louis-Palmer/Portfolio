import { href } from '$lib/utils';

const title = 'Resumé';

// const resume = href('/pdf/resume.pdf');
const resume = href('pdf/LouisPalmer_Resume.pdf');

const ResumeData = { title, resume };

export default ResumeData;
